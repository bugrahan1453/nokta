# ============================================================
# Binance Futures Trading Bot - Bağımlılıklar
# Kurulum: pip install -r requirements.txt
# ============================================================

# --- Binance API ---
python-binance==1.0.19
binance-futures-connector==3.3.0

# --- Teknik Analiz ---
numpy>=1.26.4          # pandas'tan önce kurulmalı
pandas>=2.1.4
pandas-ta>=0.3.14b
ta>=0.11.0

# --- Web Çerçevesi ---
flask==3.0.0
flask-login==0.6.3
flask-wtf==1.2.1
flask-socketio==5.3.6
Werkzeug==3.0.1
WTForms==3.1.1

# --- Async & Zamanlama ---
aiohttp==3.9.1
# asyncio - Python 3.4+ içinde gömülü, kurulum gerekmez
apscheduler==3.10.4
websocket-client==1.7.0
websockets==12.0

# --- Veritabanı ---
sqlalchemy==2.0.23
alembic==1.13.0

# --- Telegram ---
python-telegram-bot==20.7

# --- AI ---
anthropic==0.40.0

# --- Ortam & Config ---
python-dotenv==1.0.0
pydantic==2.5.3
pydantic-settings==2.1.0

# --- HTTP ---
requests==2.31.0
httpx==0.26.0
urllib3==2.1.0

# --- Yardımcı ---
python-dateutil==2.8.2
pytz==2023.3.post1
schedule==1.2.1

# --- Güvenlik ---
cryptography==41.0.7
itsdangerous==2.1.2

# --- Loglama ---
colorlog==6.8.0
loguru==0.7.2

# --- Sistem ---
psutil==5.9.7

# ============================================================
# OPSİYONEL BAĞIMLILIKLAR
# Sadece ihtiyaç duyduğunuz modüller için kurun.
# ============================================================

# --- RSS & Haber Okuma (CryptoPanic - ücretsiz, opsiyonel) ---
# pip install feedparser>=6.0.10

# --- Duygu Analizi (Temel - ücretsiz) ---
# vaderSentiment - kripto/sosyal medya için optimize VADER
# pip install vaderSentiment>=3.3.2
# TextBlob - genel duygu analizi
# pip install textblob

# --- Twitter/X ÜCRETLİ API (opsiyonel) ---
# Gereksinim: Twitter Developer hesabı + Bearer Token (aylık $100+)
# pip install tweepy>=4.14.0

# --- Twitter/X ÜCRETSİZ SCRAPER (opsiyonel, önerilen) ---
# Gereksinim: Sadece kendi Twitter hesabın (kullanıcı adı + şifre)
# .env: TWITTER_SCRAPE_ENABLED=true, TWITTER_SCRAPE_USERNAME=..., TWITTER_SCRAPE_PASSWORD=...
# pip install twscrape>=0.11.0

# --- Reddit API (opsiyonel) ---
# Gereksinim: https://www.reddit.com/prefs/apps → "script" uygulaması oluştur
# pip install praw>=7.7.1

# --- FinBERT - Finansal Duygu Analizi (opsiyonel, ~400MB) ---
# pip install transformers>=4.36.0 torch>=2.1.0

# --- Makine Öğrenmesi (opsiyonel) ---
# XGBoost
# pip install xgboost>=2.0.3
# LightGBM
# pip install lightgbm>=4.2.0
# scikit-learn (XGBoost/LightGBM ile birlikte)
# pip install scikit-learn>=1.3.2
# PyTorch - LSTM modeli için (opsiyonel, ~2GB)
# pip install torch>=2.1.0

# --- Tüm ML paketlerini bir arada kurmak için ---
# pip install xgboost lightgbm scikit-learn  (LSTM için: + torch)
